# MAT254 Lineer Cebir Notları

A static website for MAT254 Linear Algebra lecture notes. It organizes weekly HTML notes through `manifest.json`, with shared styling, search, dark mode, KaTeX rendering, and previous/next navigation.

Run locally with:

```bash
python3 -m http.server 8000
```
